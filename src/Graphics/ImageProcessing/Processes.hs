{-# LANGUAGE GADTs #-}

module Graphics.ImageProcessing.Processes (
    ImageProcess(..),

    PointProcess(..),
    IPointProcess(..),
    MiscProcess(..),
) where

import Graphics.ImageProcessing.Core.Image
